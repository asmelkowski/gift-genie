// File: frontend/e2e/setup/cleanup.setup.ts
import { test as teardown } from '@playwright/test';

teardown('cleanup', async () => {
  console.log('🧹 E2E test cleanup (placeholder for future enhancements)');
  // Future: Could add API calls to cleanup test data if needed
});
